name: ARC3 interventional separator probe
on:
  push:
    branches: [arc3-interventional-separator-probe-v1]
    paths:
      - 'kaggle/**'
      - '.github/workflows/arc3-interventional-separator-probe.yml'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: arc3-interventional-separator-${{ github.ref }}
  cancel-in-progress: true
jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      ARC_API_KEY: ''
      OPERATION_MODE: ''
      MPLBACKEND: Agg
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run quotient contracts
        run: python -m unittest kaggle.tests.test_interventional_quotient -v
      - name: Build and verify exact frozen MG-ARC5
        run: |
          mkdir -p kaggle/interventional-separator-results
          python kaggle/scripts/build_agent.py --output kaggle/interventional-separator-results/agent.py
          echo 'd9f0b95dfee14613425e8f557808e129b25e8a055ad481800b1024ce45281ead  kaggle/interventional-separator-results/agent.py' | sha256sum --check
          python -m py_compile kaggle/scripts/interventional_separator_probe.py
      - name: Install pinned SDK
        run: |
          python -m pip install arc-agi==0.9.9 arcengine==0.9.3 python-dotenv
          python -m pip freeze > kaggle/interventional-separator-results/requirements-resolved.txt
      - uses: actions/checkout@v4
        with:
          repository: arcprize/ARC-AGI-3-Agents
          ref: 4743e7d0aaae0ded0d98a89a7e282e63564cd58b
          path: kaggle/vendor/ARC-AGI-3-Agents
      - run: python kaggle/scripts/slim_framework.py
      - name: Prepare exact ls20 world
        run: |
          mkdir -p kaggle/agent
          cp kaggle/interventional-separator-results/agent.py kaggle/agent/my_agent.py
          python - <<'PY'
          from types import SimpleNamespace
          import sys
          sys.path.insert(0, 'kaggle/scripts')
          import benchmark_audit as a
          a.prepare(SimpleNamespace(
              fixtures=False,
              games='ls20',
              environments_dir='kaggle/interventional-separator-results/envs',
              manifest='kaggle/interventional-separator-results/public.json',
              max_actions=400,
          ))
          PY
      - name: Replay candidate states and probe every primitive intervention
        run: python -u kaggle/scripts/interventional_separator_probe.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: arc3-interventional-separator-probe
          path: |
            kaggle/interventional-separator-results/
            kaggle/src/metalogic_arc3/interventional_quotient.py
            kaggle/scripts/interventional_separator_probe.py
            kaggle/tests/test_interventional_quotient.py
          if-no-files-found: warn
