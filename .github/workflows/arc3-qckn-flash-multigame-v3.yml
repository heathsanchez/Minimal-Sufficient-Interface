name: ARC3 QCKN Flash Multigame V3 RED

on:
  push:
    branches: [arc3-qckn-flash-multigame-v3]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  restart-red-green:
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Run Flash compiler restart tests
        env:
          PYTHONPATH: kaggle/src
        run: python -m unittest discover -s kaggle/tests -p 'test_flash_compiler.py' -v
