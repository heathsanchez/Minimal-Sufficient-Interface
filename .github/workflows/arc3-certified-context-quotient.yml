name: ARC3 certified context quotient
on:
  push:
    branches: [arc3-certified-context-quotient-v1]
    paths:
      - 'kaggle/**'
      - '.github/workflows/arc3-certified-context-quotient.yml'
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: arc3-certified-context-quotient-${{ github.ref }}
  cancel-in-progress: true
jobs:
  qualification:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      ARC_API_KEY: ''
      OPERATION_MODE: ''
      MPLBACKEND: Agg
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run contracts
        run: python -m unittest discover -s kaggle/tests -p 'test_*.py' -v
      - name: Build standalone candidate and offline notebook
        run: |
          mkdir -p kaggle/context-quotient-results
          python kaggle/scripts/build_agent.py
          python -m py_compile kaggle/agent/my_agent.py kaggle/scripts/context_quotient_audit.py
          cp kaggle/agent/my_agent.py kaggle/context-quotient-results/candidate.py
          python kaggle/scripts/build_notebook.py
      - uses: actions/checkout@v4
        with:
          repository: heathsanchez/Minimal-Sufficient-Interface
          ref: 07fe94a80edfbb295049d4140aef14ef2b47524b
          path: prior-worktree
      - name: Rebuild exact MG-ARC5 prior
        run: |
          python prior-worktree/kaggle/scripts/build_agent.py --output "$PWD/kaggle/context-quotient-results/prior.py"
          echo 'd9f0b95dfee14613425e8f557808e129b25e8a055ad481800b1024ce45281ead  kaggle/context-quotient-results/prior.py' | sha256sum --check
      - name: Install pinned SDK
        run: python -m pip install arc-agi==0.9.9 arcengine==0.9.3 python-dotenv
      - uses: actions/checkout@v4
        with:
          repository: arcprize/ARC-AGI-3-Agents
          ref: 4743e7d0aaae0ded0d98a89a7e282e63564cd58b
          path: kaggle/vendor/ARC-AGI-3-Agents
      - run: python kaggle/scripts/slim_framework.py
      - uses: actions/checkout@v4
        with:
          repository: arcprize/ARC-AGI
          ref: f12822c4d550121c35a275008d964afbbed47d2f
          path: upstream-arc-agi
      - name: Freeze identical worlds
        run: python kaggle/scripts/context_quotient_audit.py prepare
      - name: Run candidate, exact prior and no-quotient ablation
        run: python kaggle/scripts/context_quotient_audit.py run
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: arc3-certified-context-quotient
          path: |
            kaggle/context-quotient-results/
            kaggle/agent/my_agent.py
            kaggle/notebooks/submission.ipynb
            kaggle/src/
            kaggle/scripts/
            kaggle/tests/
            kaggle/provenance/
