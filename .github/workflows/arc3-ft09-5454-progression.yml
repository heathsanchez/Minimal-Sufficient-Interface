name: ARC3 ft09 54,54 progression
on:
  push:
    branches: [arc3-ft09-5454-progression-v1]
    paths:
      - 'kaggle/**'
      - '.github/workflows/arc3-ft09-5454-progression.yml'
  workflow_dispatch:
permissions:
  contents: read
  actions: read
concurrency:
  group: arc3-ft09-5454-progression-${{ github.ref }}
  cancel-in-progress: true
jobs:
  progression:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      ARC_API_KEY: ''
      OPERATION_MODE: ''
      MPLBACKEND: Agg
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build and verify exact frozen MG-ARC5
        run: |
          mkdir -p kaggle/ft09-5454-progression-results
          python kaggle/scripts/build_agent.py --output kaggle/ft09-5454-progression-results/agent.py
          echo 'd9f0b95dfee14613425e8f557808e129b25e8a055ad481800b1024ce45281ead  kaggle/ft09-5454-progression-results/agent.py' | sha256sum --check
          python -m py_compile kaggle/scripts/ft09_5454_progression.py
      - name: Install pinned SDK
        run: |
          python -m pip install arc-agi==0.9.9 arcengine==0.9.3 python-dotenv
          python -m pip freeze > kaggle/ft09-5454-progression-results/requirements-resolved.txt
      - uses: actions/checkout@v4
        with:
          repository: arcprize/ARC-AGI-3-Agents
          ref: 4743e7d0aaae0ded0d98a89a7e282e63564cd58b
          path: kaggle/vendor/ARC-AGI-3-Agents
      - run: python kaggle/scripts/slim_framework.py
      - name: Prepare exact ft09 world
        run: |
          python - <<'PY'
          from types import SimpleNamespace
          import sys
          sys.path.insert(0, 'kaggle/scripts')
          import benchmark_audit as a
          a.AGENT = a.Path('kaggle/ft09-5454-progression-results/agent.py')
          a.prepare(SimpleNamespace(
              fixtures=False,
              games='ft09',
              environments_dir='kaggle/ft09-5454-progression-results/envs',
              manifest='kaggle/ft09-5454-progression-results/public.json',
              max_actions=400,
          ))
          PY
      - name: Download and verify final exact ft09 effect bank
        run: |
          gh api /repos/heathsanchez/Minimal-Sufficient-Interface/actions/artifacts/10560344965/zip > /tmp/ft09bank.zip
          echo '9a7d4a52f25a006abc15f76e8f4cb3e99fbfdcdb9eb9618f3cbb27c252d31c7d  /tmp/ft09bank.zip' | sha256sum --check
          mkdir -p /tmp/ft09bank
          unzip -q /tmp/ft09bank.zip -d /tmp/ft09bank
          cp /tmp/ft09bank/ft09-dynamic-bank-performance-results/dynamic-exact-effect-bank.json kaggle/ft09-5454-progression-results/dynamic-exact-effect-bank.json
      - name: Target unresolved 54,54 exact progression endpoints
        run: python -u kaggle/scripts/ft09_5454_progression.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: arc3-ft09-5454-progression
          path: |
            kaggle/ft09-5454-progression-results/
            kaggle/scripts/ft09_5454_progression.py
          if-no-files-found: warn
