# Verified Programme Frontier

This file is the authoritative human-readable state for the active recursive-discovery programme. A conversation is not authoritative. Every promoted experiment must either update this frontier or be explicitly PARKED/SUPERSEDED.

## Constitutional objective
Build and test a general developmental solver that converts externally verified insufficiency into the smallest new capability needed to continue, while simultaneously advancing the live mathematics problem.

Core loop:

PUSH -> VERIFY -> LOCALIZE CONSEQUENCE -> CHECK SCOPE -> CHECK ATTACHMENT -> ABLATE CAUSE -> RETAIN MINIMALLY -> REIFY IF COMPRESSIVE -> CHANGE FUTURE SEARCH

Promotion contract:

VERIFIED + ATTACHED + SCOPE-CORRECT + EQUALITY-COMPLETE + CAUSAL => eligible for PROMOTION.

## Live mathematical target
Advance the E677 -> E255 programme without overclaiming beyond the exact verified layer.

## Current promoted mathematical state
- The shifted saturated n=7 frontier is represented exactly by 141 partition/derangement phase states.
- The phase representation has a symbolic arbitrary-n theorem under its stated saturated cyclic assumptions and reconstructs the 141-state n=7 shifted frontier.
- The source-backed four-row T6 PAIR-KERNEL projection rejects all 141/141 phase states for every normalized A fixing 0 and each of the four canonical minimum-curvature D representatives.
- The four canonical D representatives are a lossless gauge/scalar quotient of the complete nonlinear kappa(D)=18 layer (294 labelled maps), so kappa(D)=18 is excluded from full seven-row completion.
- Scope boundary: this does NOT prove E677 -> E255 and does NOT identify the next attainable curvature layer.

## Exact live residual
Compute the exact next attainable nonlinear D-curvature layer(s), quotient them by the valid upstream gauge/symmetries, and test the cheapest four-row T6 PAIR-KERNEL attachment first. If the four-row projection ceases to contract, escalate to relative-pair TRIANGLE-COCYCLE rather than raw seven-row search.

## Parked verified dots
- `b031c52b71a860ecceb28a60a60850eedab7eb98`: exact C-component theorem for +/-3 phase language. Structurally useful, but not the highest-information live route after T6 annihilated the kappa=18 phase frontier. Re-attach only if it constrains later curvature layers or yields a reusable symbolic invariant.
- Order-9 exact SAT closure: all 24 normalized three-Bad strict-surplus forms excluded in two independent engines. Retained as finite evidence/regression, not the current size-free route.

## Retired / scoped negative laws
- Good-row renewal + unique fixer alone is insufficient.
- Marked ancestry alone produced zero causal contraction at the tested abstraction.
- SHORT-block renewal exclusions do not automatically widen to SHORT/LONG grammar.
- Distinct symbolic names are not inequalities; equality completion is mandatory before contradiction.

## Process improvements earned from failures
- ATTACHMENT is a first-class residual type: true != attached != consequential.
- Scope widening requires explicit re-verification.
- Equality completion is mandatory for partial-algebra contradiction claims.
- A verifier file not executed in CI cannot support promotion.
- Branch movement must not silently orphan state: results are PROMOTE, PARK, SUPERSEDE, REJECT, or REQUIRE_ATTACHMENT.

## Evidence anchors
- Live branch before this frontier commit: `recursive-discovery-compiler-v1` at `04551938a5b111da951b25e8f655d47f45098850`.
- T6 four-row attachment workflow: run `33725970762`, job `100554886606`, result 141 -> 0, 13/13 partition orbits killed.
- Minimum nonlinear curvature certificate: `phase_T6_min_curvature_exclusion_certificate.py` at `04551938a5b111da951b25e8f655d47f45098850`.

## Next process experiment
The repo must be able to reconstruct this frontier without chat history. `program_frontier.json` is the machine-readable source of truth. `frontier_contract.py` validates required fields, scope discipline, evidence anchors, and exactly one live residual. Future experiment workflows should consume the frontier hash/state and emit a proposed state transition rather than relying on conversational memory.

## Update rule
After every consequential verified experiment:
1. classify result as PROMOTE / PARK / SUPERSEDE / REJECT / REQUIRE_ATTACHMENT;
2. update the machine frontier and this log;
3. state exact scope and evidence;
4. replace, do not merely append to, the single live residual;
5. leave negative laws in place so failed routes are not rediscovered accidentally.
