# Verified Programme Frontier

This file is the authoritative human-readable state for the active recursive-discovery programme. Conversation history is not authoritative. Every consequential experiment must end in PROMOTE, PARK, SUPERSEDE, REJECT, or REQUIRE_ATTACHMENT and must replace the single live residual.

## Constitutional objective
Build and test a general developmental solver that converts externally verified insufficiency into the smallest new capability needed to continue, while advancing the live mathematics problem.

PUSH -> VERIFY -> LOCALIZE CONSEQUENCE -> CHECK SCOPE -> CHECK ATTACHMENT -> ABLATE CAUSE -> RETAIN MINIMALLY -> REIFY IF COMPRESSIVE -> CHANGE FUTURE SEARCH

Promotion requires: VERIFIED + ATTACHED + SCOPE-CORRECT + EQUALITY-COMPLETE + CAUSAL.

## Current promoted mathematical state
- The shifted saturated n=7 frontier is represented exactly by 141 partition/derangement phase states.
- The phase representation has a symbolic arbitrary-n theorem under its stated saturated cyclic assumptions and reconstructs the 141-state n=7 shifted frontier.
- The source-backed four-row T6 PAIR-KERNEL projection kills all 141/141 phase states in the minimum-curvature canonical-D layer.
- The full nonlinear kappa(D)=18 layer is excluded: 294 labelled D maps, covered losslessly by the four valid upstream gauge/scalar representatives.
- The exact nonlinear curvature spectrum is now verified as kappa in {18,22,24,26,30}, with labelled counts 294, 882, 1470, 1764, 588.
- The complete kappa(D)=22 layer is also excluded by the necessary four-row T6 projection. We used only the exact output-translation symmetry D -> D+c, giving 882 labelled maps -> 126 normalized maps. Across 126 x 141 = 17,766 normalized D/phase-state pairs, every pair failed for all 720 normalized A fixing 0: 0 survivors.
- Scope boundary: none of these statements alone proves E677 -> E255; kappa=24,26,30 remain live until verified.

## Exact live residual
Select the smallest nonlinear D-curvature layer in the verified spectrum not already promoted as excluded — currently kappa=24 — normalize only by proved PAIR-KERNEL symmetries, and test the necessary four-row T6 PAIR-KERNEL projection. If survivors appear, compile exactly those survivors and attach relative-pair TRIANGLE-COCYCLE before any raw seven-row search.

## Parked verified dots
- `b031c52b71a860ecceb28a60a60850eedab7eb98`: exact C-component theorem for the +/-3 phase language. Reattach only if it constrains later curvature layers or yields a reusable symbolic invariant.
- Order-9 exact SAT closure: all 24 normalized three-Bad strict-surplus forms excluded in two independent engines. Retained as finite evidence/regression, not the live size-free route.

## Retired / scoped negative laws
- Good-row renewal + unique fixer alone is insufficient.
- Marked ancestry alone produced zero causal contraction at the tested abstraction.
- SHORT-block renewal exclusions do not automatically widen to SHORT/LONG grammar.
- Distinct symbolic names are not inequalities; equality completion is mandatory before contradiction.
- A verifier that was not executed cannot support promotion.

## Process improvements earned from failures
- ATTACHMENT is a first-class residual type: true != attached != consequential.
- Scope widening requires explicit re-verification.
- Equality completion is mandatory for partial-algebra contradiction claims.
- Branch movement must not silently orphan state.
- `program_frontier.json`, not conversation memory, is the machine source of truth.
- A frontier-driven experiment must consume the live frontier and emit one classified proposed transition.
- The first conversation-free continuation has now passed: the kappa=22 workflow read the GitHub residual, independently derived the next layer, ran the verifier, and emitted PROMOTE without chat state.

## Evidence anchors
- T6 four-row kappa=18 attachment: run `33725970762`, job `100554886606`, result 141 -> 0.
- Full kappa=18 certificate commit: `04551938a5b111da951b25e8f655d47f45098850`.
- Authoritative frontier introduced: `cc0abc7f69509de6be5ffa768ba7767867e0ad20`.
- Frontier-driven kappa=22 experiment commit: `14f0f19b82be9e7172252adaad51e8d716b7e495`.
- Kappa=22 workflow: run `33728222563`, job `100561993523`, artifact `9882811488`; 882 labelled -> 126 normalized; 17,766 D/phase pairs; 0 survivors; transition PROMOTE.

## Current meta residual
The repo can now reconstruct and route the next experiment, but the first curvature probe was still bespoke to the previous kappa=18 state. Replace it with a generic frontier-driven probe that reads promoted curvature exclusions, selects the smallest unclosed spectrum layer itself, and emits the next transition. This should make repeated kappa cycles resumable without conversation-specific code edits.

## Update rule
After every consequential verified experiment:
1. classify result as PROMOTE / PARK / SUPERSEDE / REJECT / REQUIRE_ATTACHMENT;
2. update `program_frontier.json` and this log;
3. record exact scope, run/job evidence, and consumed frontier;
4. replace, do not append to, the one live residual;
5. preserve negative laws so failed routes are not rediscovered;
6. prefer generic frontier-consuming experiment machinery over bespoke next-step scripts.
