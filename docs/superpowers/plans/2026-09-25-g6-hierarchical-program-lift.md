# G6 Hierarchical Program Lift Plan

## Hypothesis

The six G6 marker events are lifecycle slots, not compressed direction codes. Their chronological roles — depart, three interior transitions, pre-arrival, arrival — inherit the exact solved G5 six-control program `XDDDTS`. Marker position reverses chronological order, so the one compiled target is `STDDDX` in spatial column order.

## Qualification

1. Extract the G5 program and six output columns only from terminal-warranted training evidence.
2. Recover the six G6 local event roles and marker ranks from legal public observations.
3. Require the exact role census and exact `XDDDTS` G5 program; otherwise return UNKNOWN with zero writes.
4. Compile one spatial target, submit once, and replay exactly twice only on progress.
5. Preserve the result as an independent branch/workflow/artifact and update ROS.

This gate does not enumerate labels, matrices, or permutations.
