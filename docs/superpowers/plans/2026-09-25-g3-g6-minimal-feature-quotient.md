# G3-G6 Minimal Feature Quotient

## Hypothesis

The source-port-mode gate supplied the missing separator but the conjunctive relation overfit. The transported meaning is the smallest subset of already observed structural coordinates that preserves every solved target consequence and covers every G6 event.

## Gate

- Expose five canonical coordinates before hashing: source glyph, destination glyph, primitive motion, endpoint component relation, source-port mode.
- Exhaust the fixed 31-member non-empty subset lattice; this is quotient selection, not target search.
- A subset survives only if its induced relation is functional on all 18 terminal-warranted G3-G5 slots and covers all six G6 events.
- Rank surviving quotients by feature count, then class count, then canonical feature name order.
- Submit once only after a total join; replay twice only after progress. Otherwise preserve an exact zero-write residual.

