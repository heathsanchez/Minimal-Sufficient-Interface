# G3-G6 Source-Port Mode Gate

## Hypothesis

The only collision surviving `event.local-trace-role` supervision is G5 `T/S` because the board endpoint relation omits whether the legal control response broadcasts across every visible source port or selects one internal port. Add exactly that palette-invariant source-port mode and no wider frame feature.

## Gate

1. Canonicalize each visible source row by its equality partition, then retain only the distinct row patterns.
2. Add `glyph_motion_components_source_port_mode` after the prior three lenses.
3. Re-observe all 18 terminal-warranted G3-G5 slots sequentially.
4. Observe the same mode at each of the six G6 marker commits on an independent zero-write route replay.
5. Submit once only if the augmented relation is functional on all 18 solved slots and covers all six G6 events. Replay twice only after progress.

## Boundaries

- Exact public `tn36-ef4dde99`; legal pixels/actions only.
- No model calls, source inspection, target permutations, or arbitrary Boolean synthesis.
- The prior full-frame `support.context@1` compiler remains rejected/uncovered and is not rerun.
- Missing coverage or any collision is `EXACT_RESIDUAL` with zero target writes and zero submits.

