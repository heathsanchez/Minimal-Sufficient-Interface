# MSI capstone gap closure — 2026-08-29

This note records what changed after `DEVELOPMENTAL_REGIME_GENESIS_CAPSTONE.md` identified four remaining arrows.

## 1. Unique minimal justified refinement — theorem added

`lean/MinimalRepair.lean` proves the universal property that was previously implicit in the meet kernel.

For the induced refinement order

\[
x \preceq y \iff x \wedge y = x,
\]

the update

\[
\boxed{E' = E \wedge K}
\]

refines both the prior interface `E` and the verified constraint `K`, and every state that refines both `E` and `K` also refines `E'`.

Therefore `E ∧ K` is the greatest lower bound in the refinement order: equivalently, the **coarsest / least-change state satisfying both requirements**. The file also proves uniqueness of any object satisfying this universal property.

This closes the relation-level form of:

\[
\boxed{\text{verified constraint} \to \text{unique minimal justified refinement}.}
\]

It does not say that an arbitrary residual uniquely determines one concrete program, constructor or morphism. Program-level uniqueness remains quotient-relative: different implementations may realize the same minimal behavioural repair.

## 2. Minimal generated regime extension — theorem added

`lean/GeneratedStage.lean` defines `adjoinStage S f`, the closure generated by:

1. every continuation already accessible in stage `S`;
2. one newly verifier-licensed morphism `f`;
3. identities;
4. everything forced by composition.

Lean proves the universal property:

> Any composition-closed stage extending `S` and admitting `f` must contain every morphism in `adjoinStage S f`.

Thus `adjoinStage S f` is the **least composition-closed regime extension** containing the new morphism.

The same file joins this to `DevelopmentalCategory.new_separator_forces_split`: if `f` separates a pair merged by the old stage, then adjoining exactly this least generated extension forces a strict behavioural-interface refinement.

This closes the formal arrow

\[
\boxed{
\text{licensed morphism}
\to
\text{least generated regime extension}
\to
\text{strict quotient refinement}.
}
\]

Together with the existing bounded endogenous-genesis experiments, the programme now has the two halves of residual-driven regime change in explicit form:

\[
\rho \to f
\quad\text{(bounded exhaustive synthesis)}
\]

and

\[
f \to \operatorname{Adjoin}(S,f) \to Q_{t+1}
\quad\text{(general Lean theorem)}.
\]

The remaining theorem-level bridge is to internalize the residual-to-`f` selection rule itself at the same abstraction level.

## 3. Observation-language genesis — bounded executable test added

`tests/test_observation_language_genesis.py` moves the uncertainty boundary above a fixed observation atom.

The initial interface contains only one observation. The learner receives no target observation identity. Instead it receives verifier-returned collision pairs and generates candidate observations from the grammar

- primitive state bits;
- negation;
- conjunction;
- xor.

A candidate is admitted only when it:

1. repairs every live verifier collision;
2. introduces no distinction not licensed by the verifier-required target quotient;
3. genuinely refines the old interface;
4. is minimal in the generated observation language.

The test ranges over every binary verifier distinction on the four-state carrier for which the initial interface has a live residual. Exact ablation removes the generated observation and restores the coarse interface.

This is a bounded form of

\[
\boxed{
\rho
\to
\text{generated observation atom}
\to
\Pi_{t+1}.
}
\]

It is stronger than selecting among a supplied list of semantic observation names, but weaker than discovering the verifier's protected objective or human intent itself.

## 4. Categorical emergence — current exact boundary

The repo already contains a one-object categorical precursor:

- `CONSTRUCTOR_GENESIS.md` generates a composition term from syntax rather than a named law;
- verifier counterexamples eliminate alternatives;
- retained composition has zero identity-law failures and zero associativity failures on the exhaustive finite census;
- `BehaviouralCongruence` and `TypedBehaviouralCongruence` then establish quotient-compatible composition and functorial descent.

Thus composition law can be operationally recovered rather than named, and the recovered operation satisfies the axioms of a one-object category (a monoid) on the reachable algebra.

What is **not** yet closed is the stronger multi-object claim:

\[
\boxed{
\text{residual development}
\to
\text{emergent object/type boundaries + typed homs + identities + composition}
}
\]

without those object/type boundaries being installed in advance.

That is now the sharp categorical frontier.

## Revised frontier

The original four gaps should now be rewritten as:

1. **relation-level unique minimal repair:** CLOSED formally;
2. **least regime extension after a licensed morphism:** CLOSED formally;
3. **observation-language genesis:** CLOSED in a bounded finite generated-language experiment; open for raw natural observation languages and task/intent discovery;
4. **categorical emergence:** composition/identity/associativity recovered in the one-object case; multi-object/type genesis remains open.

The strongest remaining capstone theorem is therefore narrower than before:

\[
\boxed{
\rho
\to
[f]_{\mathrm{beh}}^{\min}
\to
\operatorname{Adjoin}(\mathcal C_t,f)
\to
Q_{t+1}
\to
\operatorname{CapReach}_{t+1},
}
\]

with residual-to-morphism selection mechanized up to behavioural equivalence, followed by a multi-object experiment in which the typing/object structure itself must be inferred rather than supplied.

## Focused CI

`.github/workflows/capstone-gap-closure.yml` checks only the new capstone gates:

- residual-driven observation-language genesis;
- `MinimalRepair.lean`;
- `GeneratedStage.lean`;
- their required MSI Lean dependencies.
