# Minimal Sufficient Interface — Consolidated Current State

This branch is a non-destructive consolidation of the repository's 210 pre-existing branches.

## Foundational present

The consolidation is based on `proof-carrying-developmental-computer-v0` at `4425591155b3f6523c7b8aacbb48958f197b2bbb`.

That commit is a **documentation-only child** of the qualified CLC nonlinear-lineage head:

`900a73c1d386ee0eee1205a1cf31d270f3311ef7`

The dedicated CLC run **35555539801** is green. Its Lean audit contains no `sorryAx`; the reported dependencies of quotient/continuation theorems are the standard Lean foundations recorded by that run.

This makes the foundational picture:

[
	ext{qualified CLC semantics}
ightarrow
	ext{proof-carrying developmental computer specification}
]

rather than “whatever ARC-3 branch happened to be newest.”

## Independent formal lines retained

Two strong independent formal lines remain first-class evidence rather than being mechanically merged:

- **QCK finite-linear** — `fc112771...`, run **35306672847**.
- **Nucleus free warranted completion** — `486fecd...`, run **35585569597**. The free-category universal theorem is axiom-free; the quotient-presented results use `propext` and `Quot.sound` as reported by the Lean audit.

The failed earlier `nucleus-universal-property-v0` run is retained as a failed predecessor, not erased or promoted.

## ARC-3 is an application lane

ARC-3 work is valuable evidence about the calculus, but it is not the foundational MSI kernel.

Important current facts:

- **Consequential Atlas V0:** PASS. It demonstrates surface-free causal memory, residual-forced representation change, and held-out transport.
- **Consequence Nucleus V0:** 2/2 known fixtures at mean score 100, but 0 progress on the five official public games. Its own boundary says it is not leaderboard evidence.
- **Official16:** 0/16 wins, mean score 0, no technical errors. This is retained as a genuine negative public-readiness result.
- **Consequence First V1:** a competition-safe public screen, but free-model routing is explicitly a model-quality confound.
- **Stable V2:** no scientific verdict. The intended workflow failed before producing scores; null results were not analyzer/rate-limit failures.
- **Architecture Tournament:** retained as a separate application branch because it diverges after the Official16 base rather than subsuming the consequential line.

## Consolidation rule

Branches are not merged merely because they are newer or green. Formal authority, developmental semantics, application controllers, diagnostics, and negative evidence remain distinct.

The branch inventory is frozen in `evidence/consolidation/all-branches.tsv`; the machine-readable decision record is `evidence/consolidation/manifest.json`.

The consolidation itself changes no executable, Lean, Kaggle, or runtime source.
